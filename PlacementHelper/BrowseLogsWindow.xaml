<Window x:Class="PlacementHelper.BrowseLogsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Browse Logs" Height="600" Width="800" Background="#22303e" WindowStyle="SingleBorderWindow">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" CornerRadius="10"/>
    </WindowChrome.WindowChrome>
    <Border CornerRadius="10" Background="#22303e">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*"/>
                <ColumnDefinition Width="70*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Log ListBox -->
            <ListBox Name="logListBox" Grid.Column="0" Background="#34495e" Foreground="#ecf0f1"
                     SelectionChanged="LogListBox_SelectionChanged" Margin="10"/>

            <!-- Details TextBox -->
            <TextBox Name="detailsTextBox" Grid.Column="1" Background="#34495e" Foreground="#ecf0f1"
                     Margin="10" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Style="{StaticResource ModernTextBox}"/>
        </Grid>
    </Border>
</Window>
